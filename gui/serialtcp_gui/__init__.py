"""Tkinter Port Manager GUI for serial -> TCP mappings.

The desktop front-end for the ``serial-tcp-clients`` CLI/backend package,
shipped as the separate ``serial-tcp-clients-gui`` distribution.

Run with ``python -m serialtcp_gui [config.yaml]``.
"""

__version__ = '2.2.3'
